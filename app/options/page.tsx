import { ComingSoon } from "@/components/ComingSoon";

export default function OptionsPage() {
  return (
    <ComingSoon
      title="Options Desk"
      subtitle="options"
      blurb="On-chain options on your brokers and tokenized stocks — write covered calls on your desk and collect premium. Rolling out after activation goes live."
    />
  );
}
